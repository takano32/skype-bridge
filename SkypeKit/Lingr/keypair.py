# You will need to replace keyFileName with a valid keypair filename
import os
home = os.environ['HOME']
keyFileName = home + '/opt/SkypeKit/SkypeLingrBridge.crt';
distroRoot 	= home + '/opt/SkypeKit/skypekit-sdk';
